import React from 'react';
import styles from './Hero.module.css';

const Hero = () => {
  return (
    <div id="home" className={styles.hero}>
      <div>Hero</div>
    </div>
  );
}

export default Hero;
